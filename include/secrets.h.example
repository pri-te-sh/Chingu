// Copy to include/secrets.h and fill in. secrets.h is git-ignored and never leaves this machine.
#pragma once
#define WIFI_SSID      "your-network"
#define WIFI_PASSWORD  "your-password"

// Pixel's brain. Local dev: your Mac's LAN IP + port 8765, no TLS.
// Modal:  BACKEND_HOST "xxxx--pixel-brain-web.modal.run", BACKEND_PORT 443, BACKEND_TLS 1
#define BACKEND_HOST   "192.168.1.50"
#define BACKEND_PORT   8765
#define BACKEND_TLS    0
#define BACKEND_TOKEN  ""                 // must match PIXEL_TOKEN on the server (empty = no auth, local only)
